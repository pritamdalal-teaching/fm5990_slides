knitr::opts_chunk$set(echo = FALSE)
damage <- seq(0, 10000, 500)
damage
pay_off <- max(damage - strike, 0)
strike <- 1000
pay_off <- max(damage - strike, 0)
x <- c(1:5); y <- x # create some data
par(pch=22, col="red") # plotting symbol and color
par(mfrow=c(2,4)) # all plots on one page
opts = c("p","l","o","b","c","s","S","h")
for(i in 1:length(opts)){
heading = paste("type=",opts[i])
plot(x, y, type="n", main=heading)
lines(x, y, type=opts[i])
}
payoff <- max(damage - strike, 0)
plot(damage, payoff, main="insurance payoff")
payoff
damage <- seq(0, 10000, 500)
damage
payoff <- damage - strike
payoff
payoff <- pmax(damage - strike, 0)
payoff
plot(damage, payoff, main="insurance payoff")
strike <- 1000
damage <- seq(0, 5000, 500)
payoff <- pmax(damage - strike, 0)
heading <- "insurance payoff"
plot(damage, payoff, main="insurance payoff")
strike <- 1000
damage <- seq(0, 5000, 500)
payoff <- pmax(damage - strike, 0)
heading <- "insurance payoff"
plot(damage, payoff, main="insurance payoff")
lines(x, y, type="l")
strike <- 1000
damage <- seq(0, 5000, 500)
payoff <- pmax(damage - strike, 0)
heading <- "insurance payoff"
plot(damage, payoff, type="n", main="insurance payoff")
lines(damage, payoff, type="l")
strike <- 1000
damage <- seq(0, 3000, 500)
payoff <- pmax(damage - strike, 0)
heading <- "insurance payoff"
plot(damage, payoff, type="n", main="insurance payoff")
lines(damage, payoff, type="l")
